machine:
  node:
    version: 5.3.0
  services:
    - docker



dependencies:
  override:
    - npm install
    - composer install --prefer-source --no-interaction
    - gem install bundler -v 1.10.6 && bundler

test:
  override:
    - npm run build
    - npm test
