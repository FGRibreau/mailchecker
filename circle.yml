machine:
  node:
    version: 5.3.0
  php:
    version: 5.5.11

dependencies:
  php:
    - composer install --prefer-source --no-interaction

test:
  override:
    - npm test
    - phpunit test/platform.php.test.php
